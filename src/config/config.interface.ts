// need to disabled this eslint rule because we want to use the empty object type.
// will be used later for further learning

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface IConfig {}
